/* istanbul ignore next */

const ADMIN = 'ADMIN';
const PREMIUM = 'PREMIUM';
const FREE = 'FREE';
const GUEST = 'GUEST';
const ALL = [ADMIN, PREMIUM, FREE, GUEST];

const roles = {
  ADMIN,
  PREMIUM,
  FREE,
  GUEST,
  ALL
};

export default roles;
